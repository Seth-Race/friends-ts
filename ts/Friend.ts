export class Friend {

    friendName:string;
    age:number;
    email:string;
    bff:boolean;
        constructor(fn: string, a: number, e: string, b: boolean) {
            this.friendName = fn;
            this.age = a;
            this.email = e;
            this.bff = b;
        };

    
};